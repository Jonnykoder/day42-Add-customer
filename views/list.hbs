<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List</title>

    {{!-- CSS Styles --}}
    <link rel="stylesheet" href="../css/style.css">

    <!-- icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.1/font/bootstrap-icons.css">

    {{!-- Bootstrap --}}
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    {{!-- font --}}
     <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500&display=swap" rel="stylesheet">
  
</head>
<body>
    <h2 class="text-center"> {{title}}</h2>
    <h2 class="text-center"> {{message}}</h2>
    <div class="container">
        <div class="card  p-4">
            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Email</th>
                        <th>House #</th>
                        <th>Street</th>
                        <th>Barangay</th>
                        <th>City</th>
                        <th>Province</th>
                        <th>Detailed Address</th>
                        <th>Action</th>
                    </tr>
                </thead>
            
                    {{#each user}}
                        <tr>
                            <td>{{this.customer_id}}</td>
                            <td>{{this.first_name }} {{this.last_name}}</td>
                            <td>{{this.email}}</td>
                            <td>{{this.house_number}}</td>
                            <td>{{this.street_name}}</td>
                            <td>{{this.barangay}}</td>
                            <td>{{this.city}}</td>
                            <td>{{this.province}}</td>
                            <td>{{this.detailed_address}}</td>

                        
                            <td class="text-center"> 
                                <a href="/auth/updateForm/{{this.email}}" class="btn btn-warning btn-action"> <i class="bi bi-pencil" > </i>Edit</a> </button>
                                <a href="/auth/delete/{{this.email}}" onclick="return confirm('Are you sure you want to delete this record?')" class="btn btn-danger"> <i class="bi bi-trash btn-action"></i> delete</a> 
                            </td>
                        
                        </tr>
                    {{/each }}
               
                
              
        
            </table>
            {{!-- {{alertMsg}} --}}
              <a href="/addingcustomer" onclick="return confirm('Add new user?')" class="btn btn-success"> <i class="bi bi-plus-lg"></i> Add new</a> 
        </div>

    </div>
   
    
</body>
</html>